

module.exports = {
  // Path to your Sanity Studio config
  // Adjust if your studio folder or config file is named differently
  schemaPath: '../studio-portfolio/dist/schemaTypes/index.js',
  // Output path for generated types (relative to this config file)
  outputPath: 'types/generated/sanity.d.ts',
  // Optionally, specify your dataset and projectId
  dataset: 'production',
  projectId: 'sjte2cbd',
};
